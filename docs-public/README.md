# Public Docs

This directory is intended for files that can be public in Git.

Current public file:
- `metadata/hcv-devnet.json`

If you need to publish token metadata via GitHub raw URL, keep only non-sensitive files here.
